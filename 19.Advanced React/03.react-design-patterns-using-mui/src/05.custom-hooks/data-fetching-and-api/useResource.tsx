// import { useFetch } from "./use-fetch";

// export function useResource(resourceSource: "sessionStorage" | "localStorage" | "API" | "Cookie" | "BrowserDatabase", resourceIdentifier: string) {
// let data, loading, error
// switch (resourceSource) {
//     case "API":
//         const {data, loading, error} = useFetch(`api/${resourceIdentifier}`)
// }
// }

