//they are data containers so we use nouns or short phrases with adjectives

const userData:Object = {} ;
const isValid: boolean = true;

// if the variable is not boolean : describe the value: name , age, user, database
// sometimes we need provide more details without redundancy: authenticatedUser, firstName

// if the variable is boolean : answer a true/false question: isActive, loggedIn
// sometimes we need provide more details without redundancy: isActiveUser, loggedInCustomer